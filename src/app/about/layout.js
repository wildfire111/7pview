export const metadata = {
    title: "About - 7PView",
    description:
        "Learn about 7PView statistics, delta calculations, and confidence intervals for Canadian Highlander performance metrics.",
};

export default function AboutLayout({ children }) {
    return <div className="min-h-screen bg-background">{children}</div>;
}
